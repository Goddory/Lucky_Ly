import jwt from 'jsonwebtoken'; import fetch from 'node-fetch'; async function run() { const token = jwt.sign({ userId: 1 }, 'Lohocbaidikhongaicuunoimayneumaykhongchiuhocbaidautaonoithatday8888@#', { expiresIn: '1h' }); const res = await fetch('http://localhost:4000/api/events', { method: 'POST', headers: { 'Content-Type': 'application/json', 'Authorization': `Bearer ${token}` }, body: JSON.stringify({ title: 'Api test', date: new Date().toISOString(), type: 'personal_note' }) }); const text = await res.text(); console.log(res.status, text); } run();
